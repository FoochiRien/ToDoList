<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="lyadrienne.generalassemb.git.myapplication.MainActivityFirstScreen">

    <include
        android:id="@+id/toolBar"
        layout="@layout/toolbar"
        />

    <TextView
        android:id="@+id/yourliststitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Lists"
        android:textSize="40dp"
        android:paddingLeft="50dp"
        android:layout_below="@+id/toolBar"
      />

    <TextView
        android:id="@+id/emptylisttextview"
        android:layout_width="match_parent"
        android:text="Your To Do List is empty. Click the '+Add' to start."
        android:textSize="25dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:visibility="invisible"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/additem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="+ Add"
        android:textSize="20dp"
        android:background="#D1C4E9"
        android:textColor="#757575"
        android:layout_below="@+id/toolBar"
        android:layout_alignParentRight="true"
        />

    <ListView
        android:id="@+id/itemsinlist"
        android:layout_below="@id/yourliststitle"
        android:layout_width="match_parent"
        android:textAlignment="center"
        android:layout_height="wrap_content"></ListView>

</RelativeLayout>
